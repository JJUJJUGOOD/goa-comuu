# 고아 커뮤니티

경주정보고 갤러리 하나로 시작하는 커뮤니티입니다. Node.js 24 서버와 실제 SQLite DB를 사용합니다. npm 패키지는 별도로 설치하지 않아도 됩니다.

## 지금 접속하기

- 친구 공유: 실행 후 `PUBLIC-URL.txt`에 표시되는 주소
- 이 PC: http://127.0.0.1:3000
- 관리자: 사이트 오른쪽 위 톱니바퀴 / 관리 메뉴
- 초기 관리자 비밀번호: `data/owner-access.txt` 파일에서 확인하세요. 이 파일은 친구에게 보내지 마세요.
- 회원: 오른쪽 위 `회원가입`에서 아이디·닉네임·비밀번호를 만들고 `로그인`합니다.
- DB 확인·수정: 관리자 로그인 → 관리 → `회원 관리` 또는 `DB 확인·수정`

## 켜고 끄기

1. `Start-Community.cmd`를 두 번 클릭하면 Node 서버와 인터넷 공유가 시작됩니다.
2. 인터넷 주소는 창과 `PUBLIC-URL.txt`에 표시됩니다. 실행 중 다시 시작 파일을 눌러도 중복으로 실행하지 않습니다.
3. `Stop-Community.cmd`를 두 번 클릭하면 이 커뮤니티의 서버와 공유 연결만 종료합니다.

PC를 끄거나 절전 상태로 전환하면 접속이 끊깁니다. 창을 닫는 것만으로 서버가 꺼지지는 않습니다. 공유 연결을 종료했다 다시 켜면 인터넷 주소가 바뀝니다. 현재 주소는 임시 테스트용입니다. 장기 운영하려면 상시 켜진 호스팅과 고정 도메인이 필요합니다. 공유 링크를 받은 사람은 누구나 접속할 수 있습니다.

## 글과 댓글

- 비회원은 닉네임과 4자 이상의 글 비밀번호로 글과 댓글을 쓸 수 있습니다.
- 회원 글·댓글은 서버가 로그인 계정에 연결하며 별도 글 비밀번호를 입력하지 않습니다. 작성자 또는 관리자만 수정·삭제할 수 있습니다. 비회원은 작성 시 정한 비밀번호로 인증합니다.
- 일반 회원은 `운영자` 닉네임을 사용할 수 없습니다. 운영자는 로그인 후 `공지로 등록`할 수 있습니다.
- 회원 닉네임은 서버가 로그인 정보에서 결정합니다. 브라우저 입력을 조작해도 다른 닉네임으로 저장할 수 없으며 비회원은 회원 닉네임을 사용할 수 없습니다.
- 비회원 글·댓글만 작성 시 정한 비밀번호로 수정·삭제합니다.
- 사진은 글당 JPG, PNG, WebP 1장, 최대 4MB를 첨부합니다. 수정 시 기존 사진을 유지합니다.
- 본문 아래 파란 별 **개념** 버튼은 추천, 회색 화살표 **비추** 버튼은 비추천입니다. 각 숫자는 서버 DB에 별도로 저장되며 새로고침·서버 재시작 후에도 유지됩니다.
- 개념·비추는 각각 같은 브라우저 쿠키 기준으로 한 번만 참여할 수 있습니다. 기존 추천 수는 개념에 그대로 이어집니다. 비추는 개념글 선정 기준에 영향을 주지 않습니다.
- 성인 게시물 체크 시 목록·상세 제목에 19 표시가 붙습니다. 이 표시는 나이 인증 기능은 아닙니다.
- 개념글 탭은 추천 기준을 충족한 글만 모아 보여주며 제목에 개념 배지가 붙습니다.

관리자 전용 아이디는 `data/admin-account.txt`에서 확인합니다. 비밀번호는 기존 관리자 비밀번호입니다. 관리자 화면에서도 아이디와 비밀번호를 모두 입력해야 합니다. 전용 관리자 계정은 회원 관리에서 차단·강등할 수 없습니다.

업데이트 전 글은 작성자 계정 정보가 없으므로 기존 글 비밀번호 또는 관리자 인증으로 수정·삭제합니다. 닉네임이 같다는 이유로 기존 글의 소유권을 부여하지 않습니다.
- 검색, 말머리, 인기글, 공지, 페이지 이동을 지원합니다.

## 내 마음대로 꾸미기

### 갤러리 매니저 지정·양도

운영자는 관리 화면의 갤러리별 `매니저 지정`을 눌러 활성 회원의 로그인 아이디로 매니저를 지정합니다. 지정된 회원은 일반 로그인 후 상단 `매니저 관리`에서 갤러리 설정과 매니저 양도를 할 수 있습니다. 양도 즉시 이전 매니저의 권한이 사라집니다. 매니저는 해당 갤러리의 글·댓글 삭제와 공지 지정이 가능하며, 다른 회원의 글 본문 수정이나 사이트 전체 DB·회원 관리는 할 수 없습니다. 매니저가 없는 갤러리는 `미지정`으로 표시됩니다.

회원 글의 수정 버튼은 작성자와 운영자에게만 표시됩니다. 수정 주소에 직접 접속해도 서버에서 작성자 인증을 통과해야 수정 화면이 열립니다. 비회원 글도 수정 화면 전에 글 비밀번호를 확인합니다.

### 비회원 IP

비회원 글·댓글의 닉네임 뒤에 IP 일부를 표시합니다. IPv4는 앞 두 구간을 각 3자리로 표시하여 `123.045`처럼 6자리 숫자가 나오고, IPv6는 앞 6자리 16진수가 표시됩니다. 전체 IP는 비회원 글·댓글의 `author_ip`에 저장하며 운영자 DB 화면에서만 확인합니다. 회원 글·댓글에는 IP를 공개하지 않습니다. 이전에 작성되어 IP 기록이 없는 글에는 IP를 만들어 붙이지 않습니다.

실행 스크립트는 Cloudflare 터널을 신뢰하도록 구성합니다. 프록시 IP는 로컬 터널 연결에서만 수용하며 다른 연결은 실제 연결 IP를 사용합니다.

관리자 로그인 후 커뮤니티 이름, 한 줄 소개, 공간 소개, 색상, 상단 공지, 인기글 추천 기준을 바꿉니다. 갤러리를 추가하거나 이름·아이콘·소개를 수정할 수 있습니다. 게시글이 없는 갤러리만 삭제할 수 있으며 적어도 1개는 남겨야 합니다.

`회원 관리`에서는 가입 회원의 닉네임, 일반 회원·운영자 권한, 활성·차단 상태를 바꿀 수 있습니다. 운영자 권한으로 로그인한 회원도 관리자 화면에 접근할 수 있습니다.

관리자로 글을 열면 비밀번호 없이 글·댓글 삭제, 글 수정, 공지 지정·해제가 가능합니다. 초기 안내글 2개는 관리자만 수정·삭제할 수 있습니다. 관리자 로그인은 8시간 유지됩니다. 관리 화면에서 비밀번호를 변경할 수 있고, 변경 후 초기 비밀번호 안내 파일은 갱신되지 않습니다.

## 데이터 보관과 복원

- 글·댓글·설정: `data/community.sqlite`
- 첨부 이미지: `data/uploads/`
- 실행 기록: `runtime/`

관리자 화면의 **DB 확인·수정**에서는 게시글·댓글·갤러리·사이트 설정·회원 표를 확인하고 각 행의 `수정` 버튼으로 허용된 항목을 바꿀 수 있습니다. 비밀번호 해시와 로그인 세션은 화면에서 제외되고 수정할 수 없습니다. 실제 파일을 직접 열어야 할 때는 아래 경로를 사용하세요.

SQLite 화면 상단에서 연결 상태·무결성 검사 결과·저장 모드를 확인할 수 있습니다. 게시글의 작성자 회원 번호·19 표시·전체 IP, 댓글의 작성자·IP, 갤러리의 매니저 회원 번호를 표시합니다. 공지와 19 체크는 선택과 해제가 모두 저장됩니다. 회원 정보는 `회원 관리`, 매니저 양도는 `매니저 관리`에서 변경합니다. IP와 작성자 회원 번호는 기록 보존을 위해 직접 수정하지 않습니다.

관리 화면의 DB 백업 다운로드는 일관된 SQLite 스냅샷을 저장합니다. 이 백업에는 비밀번호 해시와 세션도 포함되므로 개인적으로 보관하세요. 이미지 파일은 백업 DB에 포함되지 않습니다.

전체 백업: 서버를 종료한 뒤 `data` 폴더 전체를 복사합니다. 복원: 서버를 종료하고 기존 `data` 폴더를 별도 보관한 다음 백업한 폴더를 되돌리고 시작합니다. 실행 중인 DB 파일 하나만 임의로 복사하면 최신 데이터가 빠질 수 있습니다.

## 개발

```text
node server.mjs
node --test test/*.test.mjs
```

Node.js 24 이상이 필요합니다. 기본 주소는 127.0.0.1:3000이며 `PORT`, `HOST`, `DATA_DIR`로 조정할 수 있습니다. `ADMIN_PASSWORD`는 최초 DB 생성 때만 사용됩니다. `TRUST_PROXY=1`은 신뢰할 수 있는 Cloudflare 연결을 앞에 둘 때만 설정하세요. 기본 실행 스크립트는 로컬 인터페이스에만 서버를 열고 Cloudflare 터널을 통해 HTTPS로 공유합니다.

주요 파일: `server.mjs`(HTTP/API), `db.mjs`(DB 초기화), `security.mjs`(입력·비밀번호), `public/app.js`(화면), `public/style.css`(디자인). 이 폴더를 다른 PC로 옮긴 뒤 Node.js 24 이상을 설치해서 실행할 수도 있습니다. 개인 DB와 비밀번호 파일을 공개 저장소에 올리지 마세요.
